.env
__pycache__/
*.pyc
.venv/

# Generated by the migration pipeline (large, and re-derived on every
# migrate.yml run) -- data/*_seed.json and the other small, stable files in
# data/ ARE committed (they're either source-of-truth or cheap to keep in
# sync); these two are not.
data/blog_posts_crawled.json
data/wp_import_payload.json
